//初始化Socket参数
export default class SocktParam {
    constructor(sink, resetConnect = true) {
        this.sink = sink;
        this.resetConnect = resetConnect;
    }
}
//# sourceMappingURL=SocktParam.js.map